const { window, StatusBarAlignment } = require('vscode')

const start = (config) => {

}

const stop = () => {

}

// const reset = (config) => {

// }

module.exports = {
  start,
  stop,
  // reset
}
